apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: sieve
spec:
  type: LoadBalancer  # Use LoadBalancer for external access
  selector:
    app: grafana
  ports:
  - port: 80
    targetPort: 3000
    name: http
